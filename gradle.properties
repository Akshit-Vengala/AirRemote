# AndroidX is required by the Neumorphism UI library (it pulls in
# androidx.appcompat). Existing code uses framework android.* classes, which are
# unaffected by this flag — it only allows AndroidX artifacts on the classpath.
android.useAndroidX=true
